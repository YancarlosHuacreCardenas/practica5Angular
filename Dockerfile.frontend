# Build the Angular application
FROM node:20-alpine AS builder
WORKDIR /app
COPY package*.json ./
RUN npm install
COPY . ./
RUN npm run build -- --configuration production

# Serve the built browser files with nginx
FROM nginx:stable-alpine
COPY --from=builder /app/dist/Sistemagro/browser /usr/share/nginx/html
EXPOSE 80
